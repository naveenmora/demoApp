<p id="notice"><%= notice %></p>

<h1>Listing Posts</h1>

<table id="product_lists">
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<%= link_to 'Destroy All', remove_posts_path%>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.description %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
<script type="text/javascript">
 var lastListItem = $('#product_lists:last-child table>tbody>tr:last').attr("id");
var index = 1
setInterval(function() {
    $.ajax({
    url: "/posts",
    type: "GET",
    data: {post: {
             title: "test"+index,
             description: "description"+index}
           },
    success: function(res){ 
      index++
      location.reload();
  }
});
}, 5000); 



  
</script>
